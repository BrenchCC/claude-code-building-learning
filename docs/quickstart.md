# 快速入门指南 - 从零开始学 AI 编程助手

## 🚀 10 分钟快速上手

本指南帮助你在 10 分钟内从零基础开始运行项目。

## 📋 准备工作

### 1. 安装 Python 依赖

```bash
# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate  # macOS/Linux
# 或 venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt
```

### 2. 配置环境变量

```bash
# 复制示例文件
cp .env.example .env

# 编辑 .env 文件（简单配置）
cat > .env << 'EOF'
LLM_BASE_URL=http://localhost:11434/v1
LLM_API_KEY=ollama
LLM_MODEL=llama3:latest
EOF
```

### 3. 启动 Ollama 服务

```bash
# 确保 Ollama 正在运行
ollama pull llama3:latest
```

## 🏃‍♂️ 第一次运行

### 运行 v1（最简单版本）

```bash
# 测试基础功能
python v1_bash_agent_demo/bash_agent.py "列出当前目录内容"
```

**预期输出：**
```
✅ 执行成功！
当前目录内容：
- README.md
- .env
- venv/
- v1_bash_agent_demo/
- v2_basic_agent_demo/
- v3_todo_agent_demo/
- v4_subagent_demo/
- v5_skills_agent_demo/
- requirements.txt
- docs/
```

### 运行 v2（基础功能）

```bash
# 测试文件读取功能
python v2_basic_agent_demo/basic_agent.py "读取 README.md 文件并总结内容"
```

### 运行 v3（任务管理）

```bash
# 测试任务管理功能
python v3_todo_agent_demo/todo_agent.py "请列出 3 个改进项目的建议"
```

## 🎯 常见任务示例

### 任务 1：创建文件

```bash
python v2_basic_agent_demo/basic_agent.py "创建一个名为 hello.txt 的文件，内容为 'Hello, World!'"
```

### 任务 2：查找代码

```bash
python v1_bash_agent_demo/bash_agent.py "在项目中查找包含 'todo' 关键词的文件"
```

### 任务 3：简单分析

```bash
python v4_subagent_demo/subagent.py "使用 explore 代理分析项目结构"
```

## 🐛 常见问题与解决

### 问题 1：无法连接到 LLM

**错误信息：** `ConnectionRefusedError` 或 `Invalid URL`

**解决方案：**
1. 确保 Ollama 正在运行
2. 重启 Ollama 服务
3. 检查 `.env` 文件配置是否正确

### 问题 2：模型不响应工具调用

**错误信息：** LLM 直接回答而不使用工具

**解决方案：**
1. 检查系统提示是否明确要求使用工具
2. 尝试更详细的用户提示
3. 重启 Agent

### 问题 3：任务执行失败

**错误信息：** `FileNotFoundError` 或 `Command not found`

**解决方案：**
1. 检查文件路径是否正确
2. 确保命令在系统上可用
3. 使用绝对路径

## 📊 学习进度追踪

### 第一天：基础概念

1. **理解循环结构**：运行 v1，观察基础行为
2. **工具交互**：运行 v2，学习文件操作
3. **任务管理**：运行 v3，理解 todo 机制

### 第二天：进阶功能

1. **子代理协作**：运行 v4，体验多代理模式
2. **技能系统**：运行 v5，测试技能加载
3. **性能优化**：观察执行时间和资源使用

### 第三天：实践项目

1. **项目分析**：使用 v4 Explore 代理分析代码库
2. **任务规划**：使用 v3 Todo 机制分解任务
3. **功能实现**：使用 v4 Code 代理实现功能

## 💡 快速提示

### 调试命令

```bash
# 检查 LLM 响应
python -c "from v1_bash_agent_demo.bash_agent import get_completion; print(get_completion('列出目录'))"

# 测试工具连接
python -m unittest tests.test_v1

# 查看 Agent 配置
python -c "from dotenv import load_dotenv; load_dotenv(); import os; print(os.environ.get('LLM_MODEL'))"
```

### 资源访问

- **文档**：`docs/` 目录下的详细说明
- **示例代码**：`examples/` 目录下的使用示例
- **系统提示**：`prompts/` 目录下的提示模板

### 扩展功能

```bash
# 查看所有可用技能
python -c "from v5_skills_agent_demo.skills_agent import SKILLS; print(SKILLS.list_skills())"

# 测试技能功能
python -c "from v5_skills_agent_demo.skills_agent import SKILLS; print(SKILLS.get_skill_content('pdf'))"
```

## 🎓 下一步

### 1. 深入学习文档

- [v1 文档](./v1_bash_is_everything.md)：理解基础循环
- [v2 文档](./v2_basic_agent_demo.md)：学习工具系统
- [v3 文档](./v3_todo_agent.md)：掌握任务管理
- [v4 文档](./v4_subagent.md)：理解子代理架构
- [v5 文档](./v5_skills_agent.md)：深入技能系统

### 2. 修改行为

```bash
# 编辑系统提示（改变行为）
nano prompts/v2_basic_agent.md

# 添加新工具
cp v2_basic_agent_demo/basic_agent.py my_agent.py
# 在 my_agent.py 中添加新工具函数
```

### 3. 自定义功能

```bash
# 创建新的系统提示
cat > prompts/my_agent.md << 'EOF'
你是一个专业的代码辅助工具。

## 行为准则
1. 严格使用工具执行操作
2. 每一步都要验证结果
3. 保持沟通简洁
EOF

# 运行自定义 Agent
python -c "from v2_basic_agent_demo.basic_agent import chat; print(chat('你的任务'))"
```

---

**🎉 恭喜你！** 你已经完成了快速入门。现在可以开始探索更高级的功能了。
